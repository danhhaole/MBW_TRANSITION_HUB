// Import from the actual featureATS.json file - this will be the real structure from admin
import featureData from './featureMira.json'

// Named export for compatibility with existing imports
export const roleFixtures = featureData

// Default export (used by some modules)
export default featureData